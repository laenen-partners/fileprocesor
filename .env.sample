# Server
ADDR=:3001

# PostgreSQL (required — used by DBOS for durable workflows)
DBOS_DATABASE_URL=postgres://postgres:postgres@localhost:5434/fileprocessor

# Object store
OBJECT_STORE=file
OBJECT_STORE_PATH=.data/objects
OBJECT_STORE_URL=http://localhost:3001
OBJECT_STORE_SECRET=change-me-in-production

# Gotenberg — document-to-PDF conversion (empty = log stub)
GOTENBERG_URL=http://localhost:3100

# Docling — PDF-to-markdown extraction (empty = log stub)
DOCLING_URL=http://localhost:5001

# pdf2img — PDF thumbnail generation (empty = log stub)
PDF2IMG_URL=http://localhost:3101

# ClamAV — antivirus scanning (empty = disabled, logs warning)
CLAMAV_ADDRESS=localhost:3310
